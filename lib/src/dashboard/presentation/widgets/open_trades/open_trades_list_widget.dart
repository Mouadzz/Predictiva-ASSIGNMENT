import 'package:flutter/material.dart';

class OpenTradesListWidget extends StatelessWidget {
  const OpenTradesListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
    );
  }
}
