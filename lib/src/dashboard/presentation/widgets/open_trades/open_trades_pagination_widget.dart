import 'package:flutter/material.dart';

class OpenTradesPaginationWidget extends StatelessWidget {
  const OpenTradesPaginationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('pagination'),
    );
  }
}
